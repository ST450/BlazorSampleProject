<p>
    <button class="btn btn-primary" @onclick=@(async () => await OnClickCallback.InvokeAsync(10))>
        ボタン3
    </button>
</p>

@code {
    [Parameter]
    public EventCallback<int> OnClickCallback { get; set; }
}
